# WorldMap Portfolio Project

## 1. 프로젝트 한 줄 소개

하나의 웹사이트에서 사용자가 게임 모드를 선택해 국가 위치 찾기, 국가 인구수 맞추기, AI 기반 나라 추천, 실시간 랭킹을 이용하는
`백엔드 중심 게임 플랫폼`을 만든다.

핵심은 화면이 아니라 `Spring Boot 서버가 문제 생성, 정답 판정, 점수 계산, 시도 횟수, 게임 진행 상태를 주도적으로 관리`한다는 점이다.

## 2. 이 프로젝트를 포트폴리오로 가져갈 때의 포지셔닝

이 서비스는 단순 CRUD 포트폴리오보다 아래 강점을 보여줄 수 있다.

- 게임 세션과 라운드 상태를 관리하는 서버 중심 도메인 설계
- 문제 생성, 정답 판정, 점수 계산 같은 비즈니스 로직 구현
- Redis 기반 실시간 랭킹과 캐시 전략
- SSR(Thymeleaf) 기반 페이지 렌더링과 API 혼합 구조
- LLM 기능을 붙이되, 비결정적 AI 출력은 서버에서 통제하는 설계

즉, "지도 데이터를 다루는 웹게임"이 아니라 `상태 관리가 있는 백엔드 서비스`로 보여주는 것이 중요하다.

## 3. 서비스 컨셉

### 메인 컨셉

사용자는 메인 페이지에서 원하는 모드를 선택한다.

1. 국가 위치 찾기 게임
2. 국가 인구수 맞추기 게임
3. 어울리는 나라 추천
4. 실시간 랭킹 확인

### 핵심 UX 흐름

1. 사용자가 모드를 선택한다.
2. 서버가 문제 또는 설문을 생성한다.
3. 사용자가 답변한다.
4. 서버가 정답 여부, 점수, 진행 상태를 계산한다.
5. 게임 종료 후 결과와 랭킹 반영 결과를 보여준다.

## 4. MVP 범위

처음부터 기능을 많이 넣기보다 `게임 2개 + 랭킹 + 추천 1개`를 안정적으로 만드는 것이 맞다.

### MVP 1차

- 국가 위치 찾기 게임 Level 1
- 국가 인구수 맞추기 게임 Level 1
- Redis 기반 실시간 랭킹
- 나라 추천 설문 + 추천 결과 페이지
- 게스트 닉네임 기반 플레이

### MVP 2차

- 위치 찾기 Level 2
- 인구수 맞추기 Level 2
- 회원가입 / 로그인
- 내 전적 조회
- 일간 / 전체 랭킹 분리

### 이후 확장

- 업적 시스템
- 문제 풀이 히스토리
- 관리자용 국가 데이터 관리 화면
- AI 추천 결과 저장 및 재조회

## 5. 게임 규칙 설계

기획 초기에 가장 중요한 것은 `문제를 어떻게 내고, 무엇을 정답으로 판정할지`를 명확히 하는 것이다.

### 5.1 국가 위치 찾기 게임

#### Level 1

- 서버가 국가 1개를 출제한다.
- 프론트는 세계 지도에서 클릭 입력을 받는다.
- 서버는 사용자가 클릭한 좌표와 국가 대표 좌표 또는 중심 좌표를 비교한다.
- 정답 반경 안에 들어오면 정답 처리한다.
- 대륙 힌트 또는 국가 윤곽 강조를 제공할 수 있다.

#### Level 2

- 힌트 없이 출제한다.
- 허용 반경을 더 좁게 잡는다.
- 정답이 아니어도 오차 거리(km)를 계산해 부분 점수를 줄 수 있다.

#### 서버 책임

- 출제 국가 선택
- 중복 출제 방지
- 정답 허용 반경 계산
- 라운드별 점수 계산
- 남은 문제 수, 진행 상태 관리

### 5.2 국가 인구수 맞추기 게임

#### Level 1

- 서버가 국가를 1개 제시한다.
- 사용자는 보기 4개 중 인구수 범위를 선택한다.
- 예: `1천만 미만`, `1천만~5천만`, `5천만~1억`, `1억 이상`

#### Level 2

- 사용자가 숫자를 직접 입력한다.
- 서버는 실제 인구수 대비 오차율을 계산한다.
- 오차율이 낮을수록 높은 점수를 준다.

#### 서버 책임

- 국가별 인구 데이터 제공
- 보기 생성 또는 정답 수치 비교
- 오차율 계산
- 난이도별 점수 차등

## 6. AI 추천 기능 설계

이 기능은 `LLM이 모든 판단을 직접 내리게 하면 포트폴리오 품질이 떨어질 수 있다`.
그래서 아래처럼 `결정 로직`과 `설명 로직`을 분리하는 것이 좋다.

### 추천 방식

1. 사용자가 설문에 답한다.
2. 서버가 설문 점수를 기반으로 국가 후보를 계산한다.
3. 상위 3개 국가를 추린다.
4. LLM은 그 결과를 자연어로 설명한다.

### 설문 예시

- 선호 기후: 더운 / 온화한 / 추운
- 생활 속도: 빠름 / 보통 / 여유로움
- 물가 허용 범위: 낮음 / 중간 / 높음
- 자연 vs 도시 선호
- 영어 친화도 중요 여부
- 치안 / 복지 / 음식 / 문화다양성 선호

### 추천 기능에서 서버가 주도해야 할 것

- 설문 문항 관리
- 문항별 가중치 계산
- 국가 메타데이터 기반 후보 산출
- 추천 결과 저장
- 동일 사용자 추천 기록 조회

### LLM 역할

- "왜 이 나라가 어울리는지" 설명 생성
- 1~3위 국가 비교 설명
- 여행 / 이민 / 워홀 관점별 한 줄 요약

즉, `추천 결과의 근거는 서버 데이터`, `설명은 LLM`으로 분리한다.

## 7. 랭킹 시스템 설계

랭킹은 Redis를 쓰는 이유가 분명해야 한다.

### 목표

- 점수 반영이 빠를 것
- 상위 N명 조회가 빠를 것
- 모드별 / 레벨별 / 일간 / 전체 랭킹 분리가 가능할 것

### 권장 구조

- 영속 저장: RDB에 게임 결과 저장
- 실시간 조회: Redis Sorted Set 사용

### Redis 키 예시

- `leaderboard:all:location:l1`
- `leaderboard:all:population:l1`
- `leaderboard:daily:location:l2:2026-03-22`

### 랭킹 반영 흐름

1. 게임 종료
2. 서버가 최종 점수를 확정
3. RDB에 결과 저장
4. Redis 랭킹 갱신
5. 랭킹 페이지 또는 실시간 이벤트로 반영

### 실시간 전달 방식

- 1차: 클라이언트 주기 조회
- 2차: SSE 또는 WebSocket

포트폴리오 목적이면 처음에는 `SSE 또는 짧은 주기 폴링` 중 하나만 제대로 구현해도 충분하다.

## 8. 권장 아키텍처

### 프론트

- Thymeleaf SSR
- 바닐라 JavaScript
- 지도 표현: Leaflet 또는 Three.js

초기에는 구현 난이도와 안정성을 위해 `Leaflet 우선`이 적합하다.
Three.js는 3D 지구본 연출이 목적일 때 2차 확장으로 두는 편이 안전하다.

### 백엔드

- Spring Boot 3.5.x
- Spring MVC
- Thymeleaf
- Spring Data JPA
- Validation
- Spring Security(2차)
- Redis

### 저장소

- RDB: 사용자, 국가, 게임 세션, 라운드 결과, 추천 기록
- Redis: 실시간 랭킹, 캐시, 필요 시 활성 세션 보조 저장

### 현재 구현 상태

- Spring Boot 3.5.12 + Java 21 + Gradle wrapper
- Thymeleaf SSR 메인 페이지와 기본 에러 페이지
- `application.yml`, `application-local.yml`, `application-test.yml` 프로파일 분리
- Docker Compose 기반 PostgreSQL / Redis 개발 환경 뼈대
- 공통 API 예외 응답 구조
- `country` 엔티티, 시드 검증기, 시작 시 초기 적재 로직
- World Bank API 기준 2024 인구수와 대표 좌표를 담은 국가 시드 17건
- 국가 목록 / 상세 조회 API (`/api/countries`, `/api/countries/{iso3Code}`)

## 9. 도메인 모델 초안

### 주요 엔티티

- `country`
  - 국가명, ISO 코드, 대륙, 대표 좌표, 대표 좌표 타입, 인구수, 추천 태그 정보
- `member`
  - 사용자 계정 정보
- `guest_player`
  - 게스트 닉네임 기반 플레이어
- `game_session`
  - 모드, 레벨, 상태, 총점, 시작/종료 시각
- `game_round`
  - 몇 번째 문제인지, 출제 국가, 정답 데이터, 사용자 답변, 정답 여부, 획득 점수
- `survey_submission`
  - 추천 설문 제출 이력
- `country_recommendation`
  - 설문 결과 기반 추천 국가 목록 및 LLM 설명

### 관계 예시

- 한 명의 플레이어는 여러 `game_session`을 가진다.
- 한 개의 `game_session`은 여러 `game_round`를 가진다.
- 한 번의 `survey_submission`은 여러 개의 추천 결과를 만든다.

## 10. 페이지 / 화면 초안

### 필수 페이지

- `/`
  - 메인 페이지, 모드 선택
- `/games/location/start`
  - 위치 찾기 게임 시작
- `/games/location/play`
  - 위치 찾기 진행 화면
- `/games/population/start`
  - 인구수 게임 시작
- `/games/population/play`
  - 인구수 진행 화면
- `/ranking`
  - 실시간 랭킹
- `/recommendation/survey`
  - 나라 추천 설문
- `/recommendation/result/{id}`
  - 추천 결과

### 2차 페이지

- `/login`
- `/signup`
- `/mypage`
- `/history/games`

## 11. API / 서버 인터페이스 초안

SSR을 쓰더라도 게임 진행 중에는 비동기 API가 필요하다.

### 국가 데이터

- `GET /api/countries`
  - 출제 가능한 국가 목록 조회
- `GET /api/countries/{iso3Code}`
  - 특정 국가 상세 정보 조회

### 위치 찾기

- `POST /api/games/location/sessions`
  - 게임 세션 시작
- `GET /api/games/location/sessions/{sessionId}/round`
  - 현재 문제 조회
- `POST /api/games/location/sessions/{sessionId}/answer`
  - 좌표 제출

### 인구수 맞추기

- `POST /api/games/population/sessions`
- `GET /api/games/population/sessions/{sessionId}/round`
- `POST /api/games/population/sessions/{sessionId}/answer`

### 추천

- `GET /recommendation/survey`
- `POST /api/recommendation`

### 랭킹

- `GET /api/ranking?mode=location&level=1&type=daily`
- `GET /api/ranking/stream` 또는 `GET /api/ranking/sse`

## 12. 구현 우선순위

### Step 1

- 기본 SSR 화면 뼈대 작성
- 패키지 구조 초안 고정
- 공통 예외 응답 구조와 프로파일 분리

### Step 2

- 국가 데이터 준비
- 위치 좌표 / 인구수 시드 데이터 준비
- 시드 검증과 초기 적재 구조 구현

### Step 3

- 위치 찾기 Level 1 구현
- 서버 주도 세션 / 라운드 설계
- 점수 계산 로직 구현

### Step 4

- 인구수 맞추기 Level 1 구현
- 공통 게임 세션 구조 추상화

### Step 5

- 게임 종료 결과 저장
- Redis 랭킹 연동
- 랭킹 화면 구현

### Step 6

- 설문 기반 추천 로직 구현
- LLM 설명 생성 연결

### Step 7

- 인증 / 마이페이지 / 전적 조회
- 실시간성 고도화

## 13. 패키지 구조 제안

처음부터 기능 기준으로 패키지를 나누는 것이 좋다.

- `common`
  - 공통 예외, 공통 응답, 유틸
- `country`
  - 국가 데이터 조회, 시드 적재
- `game`
  - 게임 세션, 라운드, 점수 정책, 모드별 서비스
- `ranking`
  - Redis 랭킹 처리, 조회 API
- `recommendation`
  - 설문, 추천 로직, LLM 연동
- `web`
  - Thymeleaf 컨트롤러, API 컨트롤러
- `auth`
  - 로그인, 회원, 권한 처리

핵심은 `game` 패키지 안에 공통 세션 로직과 모드별 구현을 같이 두는 것이다.

## 14. 테스트 전략

이 프로젝트는 테스트가 있어야 포트폴리오 설득력이 올라간다.

### 꼭 필요한 테스트

- 점수 계산 서비스 단위 테스트
- 위치 오차 거리 계산 테스트
- 인구수 오차율 계산 테스트
- 게임 세션 상태 전이 테스트
- 랭킹 반영 통합 테스트
- 추천 점수 계산 테스트

### 권장 테스트 구분

- 단위 테스트
  - 점수 정책, 판정 로직, 추천 계산
- 통합 테스트
  - JPA 저장, Redis 연동, 서비스 흐름
- 웹 테스트
  - 주요 페이지 응답, API 요청/응답 검증

면접에서는 "어떤 테스트가 핵심 비즈니스 리스크를 막는가"를 설명할 수 있어야 한다.

## 15. 이 프로젝트에서 면접 때 강조할 포인트

- 프론트가 아니라 서버가 게임 상태를 관리하도록 설계한 이유
- 정답 판정과 점수 계산을 서비스 계층에서 어떻게 캡슐화했는지
- Redis를 단순 캐시가 아니라 랭킹용 자료구조로 사용한 이유
- AI를 사실 판단 도구가 아니라 설명 생성 도구로 제한한 이유
- SSR + API 혼합 구조를 선택한 이유

## 16. 지금 바로 시작할 때의 현실적인 개발 순서

가장 좋은 시작 순서는 아래다.

1. 프로젝트 이름 확정
2. 국가 시드 데이터 포맷 결정(JSON/CSV)
3. `위치 찾기 Level 1`만 먼저 완성
4. 게임 세션/라운드 공통 구조 정리
5. 인구수 게임 추가
6. Redis 랭킹 연결
7. AI 추천 추가

핵심은 `지도 게임 1개를 끝까지 완성한 뒤 공통 구조를 재사용`하는 것이다.
처음부터 모든 기능을 동시에 만들면 포트폴리오보다 미완성 사이드프로젝트가 되기 쉽다.
